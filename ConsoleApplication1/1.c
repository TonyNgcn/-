#include<stdio.h>
#include<stdlib.h>
#define N 101
int main(void)
{
	int count = 0, plus = 0;
	int a[N] = { 0 }, b[N] = { 0 }, temp, n;
	while (scanf("%d%d", &n, &temp))
	{
		a[n] = n;

	}

	system("pause");
	return 0;
}